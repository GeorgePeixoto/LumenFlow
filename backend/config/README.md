# Configurações

Esta pasta armazenará arquivos de configuração da aplicação, como a conexão com o Firebase. As credenciais sensíveis serão carregadas via variáveis de ambiente e não devem ser commitadas.
